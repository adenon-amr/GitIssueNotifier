name: GitHub Issue Notifier

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run the notifier
        env:
          EMAIL: ${{ secrets.EMAIL }}
          PASSWORD: ${{ secrets.PASSWORD }}
          TOKEN: ${{ secrets.TOKEN }}
        run: python main.py

      - name: Commit updated last_checked.json
        run: |
          git config user.name "adenon-amr"
          git config user.email "adenon.amr@gmail.com"
          git add last_checked.json
          git commit -m "Update last_checked.json" || echo "No changes to commit"
          git push
